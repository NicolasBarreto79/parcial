def listar_inverso(lista):
    if len(lista) == 0:
        return []
    else:
        return [lista[-1]] + listar_inverso(lista[:-1])
    
mi_lista = [1, 2, 3, 4, 5, 6, 7]
print(listar_inverso(mi_lista))  
